package main

import "fmt"

func main() {
	var name string = "joanna"
	var age int = 30

	fmt.Printf("name has value: %v and type: %T\n", name, name)
	fmt.Printf("age has value: %v and type: %T\n", age, age)
}
